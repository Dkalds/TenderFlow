-r requirements.in

# >=9.0.3: PYSEC-2026-1845 (escalada local vía /tmp/pytest-of-{user} en UNIX).
pytest>=9.0.3,<10
pytest-cov>=5.0.0,<6
pytest-xdist>=3.5.0,<4
pytest-benchmark>=4.0.0,<5
ruff>=0.16.5,<1
mypy>=1.10.0,<2
pip-audit>=2.7.0,<3
pandas-stubs>=2.0.0,<3
types-requests>=2.31.0
types-python-dateutil>=2.8.0
pre-commit>=4.6.2,<5
detect-secrets>=1.5.0,<2
pip-tools>=7.6.1,<8
# pip-tools depende de setuptools en tiempo de ejecución. uv sí lo retiene en
# el lockfile (via --generate-hashes), pero el resolver que usa Dependabot lo
# trata como paquete "unsafe" y lo omite salvo que aparezca explícito aquí —
# sin esto, cualquier PR de Dependabot que recompile requirements-dev.txt
# rompe con "In --require-hashes mode, all requirements must have their
# versions pinned" (visto en PRs #276-278, #287/#292/#293, 2026-09-08).
setuptools>=80,<85
httpx>=0.27.0,<1
hypothesis>=6.167.1,<7
codespell>=2.3.0,<3
