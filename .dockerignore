# Dependencias de desarrollo y test
requirements-dev.txt
requirements-dev.in
requirements.in

# Base de datos local (se monta como volumen)
data/

# Variables de entorno — NUNCA en la imagen
.env
.env.*
*.env

# Artefactos de Python
__pycache__/
*.py[cod]
*.pyo
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
coverage_*.txt

# Tests (no necesarios en producción)
tests/

# Control de versiones
.git/
.gitignore

# Documentación y notas
docs/
*.md
!README.md

# Entorno virtual Python
.venv/

# IDE
.vscode/
.idea/

# Archivos temporales del proyecto
test_results.txt
_test_summary.txt

# OpenCode / agentes / secretos / dev containers
.agents/
.hypothesis/
.secrets.baseline
Makefile

# graphify: knowledge graph de tooling para agentes IA (no runtime). Sin
# excluirlo, COPY . . lo mete en la imagen (~16MB) y Trivy detecta falsos
# positivos de secret-scanning sobre IDs de nodo auto-generados que
# coinciden por azar con patrones de claves (ej. "sk_test_..." de un nodo
# como mockStreamAsk matcheando el regex de Stripe secret key).
graphify-out/
