import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Proyectos y Modulos",
};

export default function ProyectosModulosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
