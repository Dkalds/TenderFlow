import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Calidad de Datos",
};

export default function CalidadDatosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
