import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pipeline y Alertas",
};

export default function PipelineAlertasLayout({ children }: { children: React.ReactNode }) {
  return children;
}
