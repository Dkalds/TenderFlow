import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Ecosistema Partners",
};

export default function EcosistemaPartnersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
