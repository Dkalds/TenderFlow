import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tendencias CPV",
};

export default function TendenciasCpvLayout({ children }: { children: React.ReactNode }) {
  return children;
}
