import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Organos",
};

export default function OrganosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
