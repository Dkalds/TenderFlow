import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Competidores",
};

export default function CompetidoresLayout({ children }: { children: React.ReactNode }) {
  return children;
}
