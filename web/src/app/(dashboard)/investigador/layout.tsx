import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Investigador",
};

export default function InvestigadorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
