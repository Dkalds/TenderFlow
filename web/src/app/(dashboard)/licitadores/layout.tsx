import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Licitadores",
};

export default function LicitadoresLayout({ children }: { children: React.ReactNode }) {
  return children;
}
