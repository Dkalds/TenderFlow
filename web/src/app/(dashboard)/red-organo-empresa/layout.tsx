import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Red Organo-Empresa",
};

export default function RedOrganoEmpresaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
