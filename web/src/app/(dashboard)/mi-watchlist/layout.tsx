import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Mi Watchlist",
};

export default function MiWatchlistLayout({ children }: { children: React.ReactNode }) {
  return children;
}
