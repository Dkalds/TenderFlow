import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Renovaciones",
};

export default function RenovacionesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
