# .trivyignore — Excepciones documentadas para el escaneo Trivy
#
# Formato: un CVE por línea (opcionalmente con comentario).
# Sólo añadir entradas con justificación explícita.
# Revisar y actualizar trimestralmente.
#
# Ejemplo:
# CVE-2023-XXXXX  # sin fix disponible en upstream; mitigado por firewall perimetral

# starlette 1.2.0 — upgrade a 1.3.x requiere FastAPI 0.137+ y prometheus-fastapi-instrumentator
# compatible. Upgrade coordinado pendiente. Última revisión: 2026-06-18.
GHSA-537c-gmf6-5ccf  # cryptography/OpenSSL — pinned via starlette; mitigado con cryptography 48.0.1
CVE-2026-54283       # starlette form DoS — API usa JSON, no form urlencoded
CVE-2026-54282       # starlette URL path injection — mitigado por reverse proxy (Render)
